---
import type { ComponentProps } from "astro/types";
import BasePicture from "@components/Picture.astro";

type Props = ComponentProps<typeof BasePicture>;

const { class: className, ...rest } = Astro.props;
---

<figure class="max-w-150" class:list={className ?? ""}>
  <BasePicture {...rest} />
</figure>
